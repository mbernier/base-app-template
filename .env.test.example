# Test environment template
# Copy to .env.test and fill with values from `supabase status`
# Requires local Supabase running: `supabase start && supabase db push`
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54340
NEXT_PUBLIC_SUPABASE_ANON_KEY=<from supabase status: Publishable key>
SUPABASE_SERVICE_ROLE_KEY=<from supabase status: Secret key>
SESSION_SECRET=<any string 32+ chars, e.g. openssl rand -base64 32>
